#ifndef TWCS_THROTTLE_H
#define TWCS_THROTTLE_H
#include <string>
#include <vector>
using namespace std;

vector<vector<string>> twcs_throttle_axes(void);
vector<vector<string>> twcs_throttle_buttons(void);
#endif //TWCS_THROTTLE_H
