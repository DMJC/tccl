#ifndef COUGAR_THROTTLE_H
#define COUGAR_THROTTLE_H
#include <string>
#include <vector>
using namespace std;

vector<vector<string>> cougar_throttle_axes(void);
vector<vector<string>> cougar_throttle_buttons(void);
#endif //COUGAR_THROTTLE_H
